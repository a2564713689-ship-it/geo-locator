# 坐标定位仪

手机端坐标定位小工具，基于高德 JS API，输出 GCJ-02 经纬度。

## 使用

用手机浏览器打开 `geo-locator.html`（需 HTTPS），允许定位权限即可输出当前位置经纬度。

## 部署

- GitHub Pages：Settings → Pages → Source 选 main 分支，访问 `https://<用户名>.github.io/geo-locator/geo-locator.html`
